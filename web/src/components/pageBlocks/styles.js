import styled from "@emotion/styled"
import { fonts } from "../../styles/vars"

export const SansBlock = styled.div`
  margin: 120px 0px;
`

export const SansBlockTitle = styled.h2`
  font-family: ${fonts.bold};
  margin: 1em 0em;
  font-size: 1em;
`

export const PageHeader = styled.header`
  margin-bottom: 2em;
`
